[build]
  # Project lives at the repository root
  base = "."
  command = "npm run build"
  publish = "dist"

# SPA fallback routes (refresh/deep-links)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
